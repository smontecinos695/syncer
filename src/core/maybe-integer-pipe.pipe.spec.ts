import { MaybeIntegerPipePipe } from './maybe-integer-pipe.pipe';

describe('MaybeIntegerPipePipe', () => {
  it('should be defined', () => {
    expect(new MaybeIntegerPipePipe()).toBeDefined();
  });
});
